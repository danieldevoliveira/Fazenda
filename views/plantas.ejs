<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title> Suas Culturas</title>
  <link rel="icon" type="image/png" href="/favicon.png">

  <style>
    body {
      font-family: Arial, sans-serif;
      margin: 0;
      padding: 0;
      position: relative;
      background-image: url(https://pixelmaze.ca/2005/04/india/day11/tea/10d/1/82369-6.jpg);
      background-size: cover;
      background-repeat: no-repeat;
    }

    body::before {
      content: "";
      position: absolute;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      background-color: rgba(0, 0, 0, 0.5);
    }

    .menu {
      background-color: rgba(0, 0, 0, 0.5);
      position: fixed;
      width: 100%;
      top: 0;
      z-index: 1000;
    }

    nav {
      display: flex;
      justify-content: space-around;
      align-items: center;
      padding: 1em;
    }

    .menu img {
      height: 80px;
    }

    .menu a {
      display: block;
      color: #78e215;
      text-align: center;
      padding: 14px 16px;
      text-decoration: none;
      transition: color 0.3s;
      font-weight: 1000;
    }

    .menu a:hover,
    .menu a.active {
      color: #4a8212;
    }

    .content {
      position: fixed;
      left: 50%;
      transform: translate(-50%, -50%);
      text-align: center;
      color: white;
      font-size: 24px;
      margin-top: 387px;
      width: 100%;
      height: 510px;
      padding: 20px;
      background-color: rgba(0, 0, 0, 0.5);
    }

    .container {
      padding: 20px;
      background-color: rgba(30, 37, 48);
      height: 470px;
      margin-left: 50px;
      width: 1200px;
      border-radius: 15px;
    }

    .grafic {
      background-color: rgb(48, 60, 82);
      height: 472px;
      width: 800px; /* Ajustei a largura para melhor visualização */
      box-shadow: 0 4px 8px rgba(0, 0, 0, 0.5);
      border-radius: 12px;
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: center;
      margin-left: 200px; /* Ajustei a margem para melhor visualização */
    }

    h2 {
      margin-top: -40px;
      color: #78e215;
      font-weight: bolder;
      font-size: 27px;
      font-family:Verdana, Geneva, Tahoma, sans-serif;
      text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.5);
    }

    table {
      width: 100%;
      border-collapse: collapse;
      margin-top: 20px;
    }

    th, td {
      padding: 15px;
      text-align: left;
      border-bottom: 1px solid #78e215;
      color: white;
    }

    th {
      background-color: #78e215;
      color: rgb(48, 60, 82);
    }

    .regiao {
      margin-top: -30px;
    }

    a.update-link, a.delete-link {
      color: #60ad13;
      text-decoration: none;
      margin-right: 10px;
    }

    a.update-link:hover, a.delete-link:hover {
      text-decoration: underline;
    }

    button {
      background-color: #78e215;
      color: rgb(48, 60, 82);
      padding: 10px 20px;
      border: none;
      border-radius: 5px;
      cursor: pointer;
      font-weight: bolder;
      font-family:'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      margin-top: 30px;
    }

    button:hover {
      background-color: #4a8212;
    }


    
    .dropdown {
    position: relative;
    display: inline-block;
  }

    .dropdown-content {
      display: none;
      position: absolute;
      background-color: rgba(0, 0, 0, 0.5);
      box-shadow: 0 8px 16px rgba(0, 0, 0, 0.2);
      z-index: 1;
      width: 200px;
      margin-top: 0;
    }

    .dropdown-content a {
      color: #78e215;
      padding: 12px 16px;
      text-decoration: none;
      display: block;
      text-align: left;
    }

    .dropdown-content a:hover {
      background-color: #4a8212;
      color: white;
    }

    .dropdown:hover .dropdown-content {
      display: block;
    }

    .content {
      position: fixed;
      left: 50%;
      transform: translate(-50%, -50%);
      text-align: center;
      color: white;
      font-size: 24px;
      margin-top: 387px;
      width: 100%; /* Ajuste a largura conforme necessário */
      height: 510px; /* Ajuste a altura conforme necessário */
      padding: 20px; /* Adiciona espaçamento interno */
      background-color: rgba(0, 0, 0, 0.5);
    }

      img{
        height: 80px;
      }

      .content img{
        height: 110px;
        margin-top: 60px;
      }



  </style>

</head>

<body>

<div class="menu">
  <nav>
    <img src="https://i.imgur.com/eCgTJyg.png" alt= "Descrição da Imagem">
    <a href="/index" onclick="changeTab(this)">Home</a>
    <!-- <a href="/dashboard" onclick="changeTab(this)">Dashboard</a> -->
    <div class="dropdown" onmouseover="showDropdownContent()" onmouseout="hideDropdownContent()">
      <a id="regioesTitulo">Dashboard</a>
      <div class="dropdown-content" id="regioesDropdown">
        <% for (const regiao of regioes) { %>
          <a href="/dashboard/<%= regiao.id %>" onclick="changeTab(this)"><%= regiao.nome %></a>
        <% } %>
      </div>
    </div>
    <a href="/plantacao" onclick="changeTab(this)">Plantações</a>
    <a href="/cadastroPlantas" onclick="changeTab(this)">Plantas</a>
  </nav>
</div>

<div class="content">
  <div class="container">
    <div class="grafic">
      <br>
      <br>
      <h2>Lista de Plantas</h2>
      <% if (plantas && plantas.length > 0) { %>
        <table>
          <thead>
            <tr>
              <th>Nome</th>
              <th>Temperatura Basal</th>
              <th>TGD</th>
              <th>Ações</th>
            </tr>
          </thead>
          <tbody>
            <% plantas.forEach(planta => { %>
              <tr>
                <td><%= planta.nome %></td>
                <td><%= planta.temp_basal %></td>
                <td><%= planta.gd %></td>
                <td>
                  <a href="/plantas/update/<%= planta.id %>" class="update-link">Update</a>
                  <a href="/plantas/delete/<%= planta.id %>" class="delete-link">Delete</a>
                </td>
              </tr>
            <% }); %>
          </tbody>
        </table>
      <% } else { %>
        <p>Nenhuma planta encontrada.</p>
      <% } %>

      <button class="suas-plantas" onclick="location.href='/cadastroPlantas'">VOLTAR</button>
    </div>
  </div>
</div>

</body>
</html>
